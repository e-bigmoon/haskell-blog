name: 'nightly dependencies check'

# on:
#   schedule:
#     - cron: "00 15 * * *"
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - '*'

jobs:
  deps-check:
    runs-on: ubuntu-latest
    steps:
      - uses: waddlaw/cabal-outdated-action@main